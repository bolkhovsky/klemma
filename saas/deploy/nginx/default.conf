upstream klemma_api {
    server api:8000;
}

server {
    listen 80;
    server_name _;

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect all HTTP to HTTPS (uncomment after TLS setup)
    # location / {
    #     return 301 https://$host$request_uri;
    # }

    # API proxy (HTTP mode — use until TLS is configured)
    location / {
        proxy_pass http://klemma_api;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Security headers
        add_header X-Content-Type-Options nosniff always;
        add_header X-Frame-Options DENY always;
        add_header Referrer-Policy strict-origin-when-cross-origin always;

        # Timeouts for long-running AI operations
        proxy_read_timeout 180s;
        proxy_connect_timeout 10s;
    }
}

# HTTPS server (uncomment after running certbot)
# server {
#     listen 443 ssl http2;
#     server_name klemma.example.com;
#
#     ssl_certificate /etc/letsencrypt/live/klemma.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/klemma.example.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Content-Type-Options nosniff always;
#     add_header X-Frame-Options DENY always;
#
#     location / {
#         proxy_pass http://klemma_api;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_read_timeout 180s;
#     }
# }
