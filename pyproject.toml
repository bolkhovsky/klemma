[project]
name = "klemma"
version = "0.18.5"
description = "AI-powered CLI assistant for PhD dissertation research — manages literature, extracts citations, generates plans"
requires-python = ">=3.11"
readme = "README.md"
license = {text = "Polyform-Noncommercial-1.0.0"}
authors = [{name = "Ilya Bolkhovskiy"}]
keywords = ["academic", "dissertation", "research", "zotero", "obsidian", "ai", "pdf", "citations"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering",
    "License :: Free for non-commercial use",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "click==8.3.3",
    "pydantic==2.13.3",
    "PyMuPDF==1.27.2.2",
    "Jinja2==3.1.6",
    "PyYAML==6.0.3",
    "rich==15.0.0",
    "requests==2.34.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=4.0",
    "ruff>=0.8",
]
openai = ["openai>=1.0"]
litellm = ["litellm>=1.40"]
embeddings = ["numpy>=1.24"]
local-embeddings = ["numpy>=1.24", "sentence-transformers>=2.2"]
docx = ["python-docx>=1.0"]
mcp = ["mcp>=1.0"]
api = [
    "fastapi==0.136.0",
    "uvicorn[standard]>=0.34",
    "argon2-cffi==25.1.0",
    "PyJWT[crypto]==2.12.1",
    "pydantic[email]>=2.0",
    "httpx>=0.27",
    "python-multipart>=0.0.9",
    "redis>=5.0",
    "rq>=2.0",
    "sqlite-vec>=0.1.9,<0.2",
]
recommended = ["litellm>=1.40"]
all-ai = ["openai>=1.0", "litellm>=1.40"]

[project.scripts]
klemma = "klemma.cli:main"

[project.urls]
Homepage = "https://github.com/klemma-ai/klemma"
Repository = "https://github.com/klemma-ai/klemma"
"Bug Reports" = "https://github.com/klemma-ai/klemma/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/klemma"]
exclude = ["src/klemma/api"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
